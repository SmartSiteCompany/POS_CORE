<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Producto</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container vintage mt-5">
    <h1>✏️ Editar producto</h1>

    <% if (errors && errors.length > 0) { %>
      <div class="alert alert-danger">
        <ul>
          <% errors.forEach(err => { %>
            <li><%= err.msg %></li>
          <% }) %>
        </ul>
      </div>
    <% } %>

    <form action="/products/edit/<%= product._id %>" method="POST">
      <div class="mb-3">
        <label class="form-label">Nombre*</label>
        <input type="text" name="name" value="<%= product.name %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Precio por pieza*</label>
        <input type="number" step="0.01" name="pricePieza" value="<%= product.pricePieza %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Precio mayoreo</label>
        <input type="number" step="0.01" name="priceMayoreo" value="<%= product.priceMayoreo %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Precio paquete</label>
        <input type="number" step="0.01" name="pricePaquete" value="<%= product.pricePaquete %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Precio 4</label>
        <input type="number" step="0.01" name="price4" value="<%= product.price4 %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Precio 5</label>
        <input type="number" step="0.01" name="price5" value="<%= product.price5 %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Categoría*</label>
        <input type="text" name="category" value="<%= product.category %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Stock</label>
        <input type="number" name="stock" min="0" value="<%= product.stock %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Código de barras</label>
        <input type="text" name="barcode" value="<%= product.barcode %>" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Descripción</label>
        <textarea name="description" class="form-control" rows="2"><%= product.description || '' %></textarea>
      </div>

      <button class="btn btn-warning">💾 Actualizar</button>
      <a href="/products" class="btn btn-secondary">Cancelar</a>
    </form>
  </div>
</body>
</html>
