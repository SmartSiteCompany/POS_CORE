<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>⚙️ Configuración del Negocio</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --morado-oscuro: #3B2A66;
      --morado-claro: #A47ED7;
      --morado-medio: #6B2E84;
      --fondo-lila: #f4f0ff;
    }

    body {
      background: linear-gradient(to right, var(--morado-claro), var(--morado-oscuro));
      font-family: 'Segoe UI', sans-serif;
      color: #333;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 30px;
    }

    .card-config {
      background-color: rgba(255, 255, 255, 0.9);
      border-radius: 1rem;
      padding: 3rem 2.5rem;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 700px;
    }

    h2 {
      color: var(--morado-oscuro);
      font-weight: bold;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    label {
      font-weight: bold;
    }

    .form-control {
      border-radius: 0.5rem;
    }

    .btn-primary {
      background-color: var(--morado-medio);
      border-color: var(--morado-medio);
      font-weight: bold;
      padding: 0.6rem 1.2rem;
    }

    .btn-primary:hover {
      background-color: #552065;
      border-color: #552065;
    }

    .alert {
      border-radius: 0.5rem;
    }
  </style>
</head>
<body>

  <div class="card-config">
    <%- include('../partials/header') %>

    <h2>⚙️ Configuración del Negocio</h2>

    <% if (typeof successMessage !== 'undefined') { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= successMessage %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <form action="/config" method="POST">
      <div class="mb-3">
        <label class="form-label">Nombre del Negocio</label>
        <input type="text" name="businessName" class="form-control" required value="<%= config?.businessName || '' %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Nombre en el Ticket</label>
        <input type="text" name="ticketName" class="form-control" required value="<%= config?.ticketName || '' %>">
      </div>

      <div class="mb-4">
        <label class="form-label">Impuesto (%)</label>
        <input type="number" step="0.01" name="taxRate" class="form-control" required value="<%= config?.taxRate || '' %>">
      </div>

      <div class="d-flex justify-content-between">
        <a href="/products" class="btn btn-secondary">↩️ Cancelar</a>
        <button class="btn btn-primary">💾 Guardar Configuración</button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <% if (typeof successMessage !== 'undefined') { %>
    <script>
      setTimeout(() => {
        window.location.href = "/products";
      }, 2500);
    </script>
  <% } %>
</body>
</html>
