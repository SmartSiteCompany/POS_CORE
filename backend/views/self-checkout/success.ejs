<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>✅ Compra Exitosa - Autocobro</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #e9e6f6, #f7f5fc);
      font-family: 'Segoe UI', sans-serif;
      color: #333;
      padding-top: 40px;
    }

    .card-success {
      background: rgba(255, 255, 255, 0.88);
      border-radius: 1rem;
      padding: 2rem;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 0 25px rgba(180, 160, 255, 0.2);
    }

    h2 {
      color: #4f3b8c;
    }

    h4 {
      color: #7446b6;
      margin-top: 1.5rem;
    }

    ul {
      list-style-type: none;
      padding-left: 0;
    }

    ul li {
      padding: 4px 0;
      border-bottom: 1px solid #eee;
    }

    .qr-box {
      margin-top: 2rem;
      text-align: center;
    }

    .qr-box p {
      margin-bottom: 1rem;
    }

    .text-success {
      color: #06d6a0 !important;
    }
  </style>

  <script>
    window.addEventListener('DOMContentLoaded', () => {
      fetch('/self-checkout/qr-ticket')
        .then(res => res.text())
        .then(html => {
          document.getElementById('qrContenedor').innerHTML = html;
        })
        .catch(err => {
          console.error('❌ Error mostrando QR:', err);
          document.getElementById('qrContenedor').innerHTML = '<p class="text-danger">No se pudo generar el QR.</p>';
        });

      // Redirigir automáticamente después de 5 segundos
      setTimeout(() => {
        window.location.href = '/self-checkout/products';
      }, 5000);
    });
  </script>
</head>
<body>
  <div class="card-success text-center">
    <h2 class="text-success">✅ ¡Gracias por tu compra!</h2>
    <p><strong>🗓 Fecha:</strong> <%= ticket.fecha %></p>

    <h4>🧾 Productos:</h4>
    <ul>
      <% ticket.cart.forEach(item => { %>
        <li>
          <%= item.product.name %> x <%= item.quantity %> — $<%= (item.product.pricePieza * item.quantity).toFixed(2) %>
        </li>
      <% }) %>
    </ul>

    <p><strong>Subtotal:</strong> $<%= ticket.subtotal.toFixed(2) %></p>
    <p><strong>Impuestos (16%):</strong> $<%= ticket.tax.toFixed(2) %></p>
    <h4><strong>Total:</strong> $<%= ticket.total.toFixed(2) %></h4>

    <p><strong>Método de pago:</strong> <%= ticket.metodo %></p>
    <p><strong>Pagado:</strong> $<%= ticket.montoIngresado.toFixed(2) %></p>
    <p><strong>Cambio:</strong> $<%= ticket.cambio.toFixed(2) %></p>

    <div id="qrContenedor" class="qr-box">
      <p>🔄 Generando código QR para tu ticket...</p>
    </div>
  </div>
</body>
</html>
