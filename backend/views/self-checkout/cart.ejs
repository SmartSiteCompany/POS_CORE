<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>🛒 Carrito - Autocobro</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(to right, #f3efff, #e8ddf9);
      font-family: 'Segoe UI', sans-serif;
      padding-top: 30px;
      min-height: 100vh;
      color: #333;
    }

    .container {
      background: rgba(255, 255, 255, 0.85);
      border-radius: 1rem;
      padding: 2rem;
      box-shadow: 0 0 20px rgba(116, 70, 182, 0.2);
    }

    h2 {
      color: #4f3b8c;
    }

    .product-img {
      max-height: 60px;
      object-fit: contain;
    }

    .table {
      background-color: #faf6ff;
    }

    .table th {
      background-color: #e5daf9;
      color: #4e2182;
    }

    .table td {
      background-color: #fff;
      vertical-align: middle;
    }

    .btn-sm-square {
      width: 32px;
      height: 32px;
      padding: 0;
      line-height: 1.2;
      border-radius: 8px;
    }

    .btn-warning {
      background-color: #ffd166;
      border: none;
      color: #333;
    }

    .btn-success {
      background-color: #06d6a0;
      border: none;
    }

    .btn-danger {
      background-color: #ef476f;
      border: none;
    }

    .btn-secondary {
      background-color: #ced4da;
      color: #333;
      border: none;
    }

    .btn-primary {
      background-color: #7446b6;
      border: none;
    }

    .btn-lg {
      font-size: 1.2rem;
      padding: 0.75rem 2rem;
      border-radius: 2rem;
    }

    .text-end p,
    .text-end h4 {
      margin-bottom: 0.2rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="mb-4 text-center">🧾 Resumen de tu carrito</h2>

    <% if (cart.length === 0) { %>
      <div class="alert alert-warning text-center">⚠️ Tu carrito está vacío.</div>
      <div class="text-center">
        <a href="/self-checkout/products" class="btn btn-primary mt-3">
          ⬅ Volver a productos
        </a>
      </div>
    <% } else { %>
      <table class="table table-bordered align-middle">
        <thead>
          <tr>
            <th>Imagen</th>
            <th>Producto</th>
            <th>Precio</th>
            <th>Cantidad</th>
            <th>Stock</th>
            <th>Subtotal</th>
            <th>Acción</th>
          </tr>
        </thead>
        <tbody>
          <% cart.forEach(item => { %>
            <tr>
              <td>
                <% if (item.product.image) { %>
                  <img src="/uploads/<%= item.product.image %>" class="product-img" />
                <% } else { %>
                  <img src="https://via.placeholder.com/60?text=Sin+imagen" class="product-img" />
                <% } %>
              </td>
              <td><%= item.product.name %></td>
              <td>$<%= item.product.pricePieza.toFixed(2) %></td>
              <td><%= item.quantity %></td>
              <td><%= item.product.stock %></td>
              <td>$<%= (item.product.pricePieza * item.quantity).toFixed(2) %></td>
              <td>
                <form method="POST" action="/self-checkout/cart/decrease/<%= item.product._id %>" class="d-inline">
                  <button class="btn btn-sm btn-warning btn-sm-square">➖</button>
                </form>
                <form method="POST" action="/self-checkout/cart/increase/<%= item.product._id %>" class="d-inline">
                  <button class="btn btn-sm btn-success btn-sm-square">➕</button>
                </form>
                <form method="POST" action="/self-checkout/remove-from-cart/<%= item.product._id %>" class="d-inline">
                  <button class="btn btn-sm btn-danger btn-sm-square">🗑️</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="text-end mb-4">
        <p><strong>Subtotal:</strong> $<%= subtotal.toFixed(2) %></p>
        <p><strong>Impuestos (16%):</strong> $<%= tax.toFixed(2) %></p>
        <h4><strong>Total:</strong> $<%= total.toFixed(2) %></h4>
      </div>

      <div class="d-flex justify-content-between">
        <a href="/self-checkout/products" class="btn btn-secondary">
          ⬅ Seguir comprando
        </a>
        <a href="/self-checkout/payment" class="btn btn-success btn-lg">
          💳 Ir a pagar
        </a>
      </div>
    <% } %>
  </div>
</body>
</html>
