<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Usuarios</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    :root {
      --morado-oscuro: #3B2A66;
      --morado-medio: #6B2E84;
      --morado-claro: #A47ED7;
      --lila-fondo: #f5efff;
      --violeta-suave: #ece3ff;
    }

    body {
      background: linear-gradient(to right, var(--morado-claro), var(--morado-oscuro));
      min-height: 100vh;
      padding-top: 40px;
      font-family: 'Segoe UI', sans-serif;
    }

    .card-glass {
      background: rgba(255, 255, 255, 0.6);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-radius: 1rem;
      box-shadow: 0 0 30px rgba(0, 0, 0, 0.15);
      padding: 2rem;
    }

    h1 {
      color: var(--morado-oscuro);
      font-weight: bold;
    }

    .btn-custom {
      padding: 8px 20px;
      border-radius: 50px;
      font-weight: bold;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background-color: var(--morado-oscuro);
      border-color: var(--morado-oscuro);
    }

    .btn-primary:hover {
      background-color: #2a1f4e;
      border-color: #2a1f4e;
    }

    .btn-warning {
      background-color: var(--morado-claro);
      border: none;
      color: white;
    }

    .btn-warning:hover {
      background-color: #8c5dc7;
    }

    .btn-danger {
      background-color: var(--morado-medio);
      border: none;
    }

    .btn-danger:hover {
      background-color: #54256c;
    }

    .table {
      background: var(--violeta-suave);
      border-radius: 0.75rem;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(116, 70, 182, 0.1);
    }

    .table th {
      background: linear-gradient(90deg, #7446b6, #8363d0);
      color: white;
      font-weight: 600;
      text-align: center;
      border: none;
    }

    .table td {
      background-color: #fdfbff;
      border: none;
      text-align: center;
      vertical-align: middle;
    }

    .table tbody tr:nth-child(even) td {
      background-color: #f3ebff;
    }

    table img {
      border-radius: 8px;
      border: 2px solid #eee;
    }

    .text-end .btn {
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="container card-glass">
    <%- include('../partials/header') %>

    <h1 class="mb-4 text-center">👥 Usuarios del Sistema</h1>

    <div class="text-end">
      <a href="/users/new" class="btn btn-primary btn-custom">➕ Nuevo Usuario</a>
    </div>

    <table class="table table-bordered align-middle">
      <thead class="text-center">
        <tr>
          <th>Imagen</th>
          <th>Nombre</th>
          <th>Email</th>
          <th>Rol</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(user => { %>
          <tr>
            <td class="text-center">
              <% if (user.image) { %>
                <img src="/uploads/<%= user.image %>" width="60" height="60" alt="Foto">
              <% } else { %>
                <span class="text-muted">Sin imagen</span>
              <% } %>
            </td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.role %></td>
            <td class="text-center">
              <a href="/users/edit/<%= user._id %>" class="btn btn-sm btn-warning btn-custom">✏️ Editar</a>
              <form action="/users/delete/<%= user._id %>" method="POST" style="display:inline;">
                <button class="btn btn-sm btn-danger btn-custom" onclick="return confirm('¿Eliminar este usuario?')">🗑️ Eliminar</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
